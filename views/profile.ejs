<% include header %>

    <title>
        <%= title %>
    </title>
    </head>

    <body>
        <div class="container">

            <div class="text-center mt-5">
                <h1>
                    <%= title %>
                </h1>
            </div>

            <div class="col-md-4 col-sm-12" style="margin: auto;">
                

                <div class="card mt-3">
                    <% if (user.image.path == undefined){ 
                        user.image.path = '/images/img_avatar.png' } %>
                    <img class="card-img-top" src="<%= user.image.path %>" alt="image">
                    <div class="card-body">
                        <p class="card-text">
                            <%= user.name %>
                        </p>

                        <small class="text-muted">
                            <%= user.email %>
                        </small>
                    </div>

                </div>

                <div class="text-center">
                    <a href="/logout" class="btn btn-warning m-3">
                        <span class="oi oi-account-logout"></span> logout</a>
                </div>


            </div>
        </div>
    </body>

    </html>